# GitHub personal-access token with `repo` scope.
# Used by Gravelord for both tracker reads/writes and HTTPS git clones.
GITHUB_TOKEN=ghp_replace_me

# Optional override for the workflow file path. Defaults to ./WORKFLOW.md.
# SYMPHONY_WORKFLOW=/etc/gravelord/WORKFLOW.md
